import { Model } from 'mongoose';
import { CreateCourseDto } from './dto/create-course.dto';
import { UpdateCourseDto } from './dto/update-course.dto';
import { CourseSchemaClass } from './schema/course.schema';
import { FilterCourseDto, SortCourseDto } from './dto/query-course.dto';
import { IPaginationOptions } from '../utils/types/pagination-options';
import { CourseEntity } from './domain/course';
import { NullableType } from '../utils/types/nullable.type';
import { PaginationResult } from '../utils/mongoose-query-builder';
import { MailService } from '../mail/mail.service';
import { ConfigService } from '@nestjs/config';
import { AllConfigType } from '../config/config.type';
import { CategoriesService } from '../category/categories.service';
import { NotificationDocument } from '../notification/schema/notification.schema';
export declare class CoursesService {
    private readonly courseModel;
    private readonly mailService;
    private readonly configService;
    private readonly categoriesService;
    private readonly notificationModel;
    constructor(courseModel: Model<CourseSchemaClass>, mailService: MailService, configService: ConfigService<AllConfigType>, categoriesService: CategoriesService, notificationModel: Model<NotificationDocument>);
    private generateSlug;
    private ensureUniqueSlug;
    private convertTimeToMinutes;
    private calculateInclusiveDays;
    private calculateTimeBlockMinutes;
    private calculateTotalSessionMinutes;
    private map;
    create(dto: CreateCourseDto): Promise<CourseEntity>;
    findManyWithPagination({ filterOptions, sortOptions, paginationOptions, }: {
        filterOptions?: FilterCourseDto | null;
        sortOptions?: SortCourseDto[] | null;
        paginationOptions: IPaginationOptions;
    }): Promise<PaginationResult<CourseEntity>>;
    findById(id: CourseEntity['id']): Promise<NullableType<CourseEntity>>;
    findBySlug(slug: string): Promise<NullableType<CourseEntity>>;
    update(id: string, dto: UpdateCourseDto): Promise<CourseEntity | null>;
    remove(id: string): Promise<void>;
    findByCategory(categorySlug: string, paginationOptions: IPaginationOptions): Promise<PaginationResult<CourseEntity>>;
    findBySubcategory(subcategory: string, paginationOptions: IPaginationOptions): Promise<PaginationResult<CourseEntity>>;
    getCoursesByCategories(): Promise<any>;
}
